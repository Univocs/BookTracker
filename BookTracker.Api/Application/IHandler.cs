namespace BookTracker.Api.Application;

public interface IHandler;

/*
    Deliberately empty Interface, meant purely as a label. 
    Any class that says : IHandler is announcing "I belong to the group called handlers"
    Think of it like a sticky note you attach to certain classes.
*/